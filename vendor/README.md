# Vendored Dependencies

Place these files here:

## Mongoose (required for C server)

Download from https://github.com/cesanta/mongoose/releases

Required files:
- `mongoose.c`
- `mongoose.h`

## Build

The Makefile expects `mongoose.c` and `mongoose.h` in this directory.
